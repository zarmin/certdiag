# certdiag

X.509 diagnostic CLI and TUI.

Source lives in `certdiag_app/`; see `certdiag_app/Makefile` and `Taskfile.yml`
for build and test targets. The CLI test harness is under `tools/testing`.

Licensed under the Apache License 2.0, see `LICENSE` and `NOTICE`.
